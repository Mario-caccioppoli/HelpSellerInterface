dist: trusty
sudo: required

language: node_js
node_js:
  - '14.17.3'

cache:
  directories:
    - ./node_modules

install:
  - npm install

script:
  - ng lint
  - npm run test-headless
  - ng build --prod
